import { Getworkcenterresponse } from './getworkcenterresponse';

describe('Getworkcenterresponse', () => {
  it('should create an instance', () => {
    expect(new Getworkcenterresponse()).toBeTruthy();
  });
});
