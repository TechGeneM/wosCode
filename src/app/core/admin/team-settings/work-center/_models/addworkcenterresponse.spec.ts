import { Addworkcenterresponse } from './addworkcenterresponse';

describe('Addworkcenterresponse', () => {
  it('should create an instance', () => {
    expect(new Addworkcenterresponse()).toBeTruthy();
  });
});
