import { Getagentroleresponse } from './getagentroleresponse';

describe('Getagentroleresponse', () => {
  it('should create an instance', () => {
    expect(new Getagentroleresponse()).toBeTruthy();
  });
});
