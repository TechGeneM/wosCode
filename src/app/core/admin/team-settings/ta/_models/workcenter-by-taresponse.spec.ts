import { WorkcenterByTaresponse } from './workcenter-by-taresponse';

describe('WorkcenterByTaresponse', () => {
  it('should create an instance', () => {
    expect(new WorkcenterByTaresponse()).toBeTruthy();
  });
});
